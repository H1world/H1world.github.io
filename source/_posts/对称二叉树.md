---
title: 对称二叉树
categories: 随笔
tags: [Js,算法,数据结构,tree]
---
请实现一个函数，用来判断一棵二叉树是不是对称的。如果一棵二叉树和它的镜像一样，那么它是对称的。
<!--more-->
### 对称二叉树
```js
/**
 * Definition for a binary tree node.
 * class TreeNode {
 *     val: number
 *     left: TreeNode | null
 *     right: TreeNode | null
 *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.left = (left===undefined ? null : left)
 *         this.right = (right===undefined ? null : right)
 *     }
 * }
 */
 function isSymmetric(root: TreeNode | null): boolean {
  return imageFn(root, root) // 传入两次并且递归对比
 }

function imageFn(l: TreeNode | null, r: TreeNode | null) {
  if(!l && !r) return true
  if(!l || !r) return false
  return l.val == r.val && imageFn(l.left, r.right) && imageFn(l.right, r.left)
}
 ```